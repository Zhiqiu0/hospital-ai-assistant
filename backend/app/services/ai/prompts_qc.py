"""
质控 Prompt 库（app/services/ai/prompts_qc.py）

包含病历质量控制相关的 prompt 模板：
  QC_PROMPT         : 综合质控——LLM 质量建议（旁路提示，不参与总分）。
                      标准段 {standard_block} 由 _qc_prompt_gen 从法定评分表
                      常量运行时生成（2026-08-12 双真相源收口：评分表是唯一
                      真相源，提示词不再手写内嵌一份标准）
  QC_FIX_PROMPT     : 单条质控修复——针对某一字段的具体问题生成修复文本，
                      供医生直接复制写入病历

调用来源：
  QC_PROMPT         → qc_stream_service.run_quick_qc_stream（LLM 旁路建议）
  QC_FIX_PROMPT     → _qc_ops.run_qc_fix

历史说明：GRADE_SCORE_PROMPT 已删（2026-08-12）——甲级评分早已由
qc_engine 规则引擎驱动（_qc_ops.run_grade_score），该 prompt 零调用。
"""

QC_PROMPT = """你是病历质控专家。系统已按下方法定评分标准用规则引擎完成结构化扣分，
你的任务是**补充规则引擎覆盖不到的质量建议**（表述规范、临床逻辑、内容矛盾、书写质量），
并对照法定标准提示潜在问题。你的输出不参与评分，仅作为给医生的改进建议。

病历类型：{record_type}
病历内容：
{content}

请输出JSON格式：
{{
  "issues": [
    {{
      "risk_level": "high/medium/low",
      "field_name": "字段名称",
      "issue_description": "问题描述（具体指出哪里有问题）",
      "suggestion": "修改建议",
      "score_impact": "影响分值说明"
    }}
  ],
  "summary": "总体评价（1-2句话，描述主要问题和改进方向，不要包含分数估计）",
  "pass": true/false
}}

{standard_block}

━━━ 判定提醒（防误报）━━━
- 门急诊病历标准相对宽松：辅助检查写"暂无"或"未行检查"合规；不要求鉴别诊断、
  专项评估（疼痛NRS/营养/VTE/康复等）、个人史/婚育史/家族史；预防接种史不详、
  职业不详不作为问题；诊断写通用诊断即可，不强制细化
- 仅当病历含有"中医诊断""中医治疗""中药""针灸""推拿"等明确中医诊疗内容时，
  才检查舌象/脉象/证候诊断/治则治法；仅因出现"饮食""方法""汤方"等普通词汇，
  不得判定为中医病历
- 客观测量数值（生命体征/检验检查结果）缺失时只建议补充，不得编造具体数值

risk_level说明：
- high：对应单项否决或扣分≥2分级别的严重问题
- medium：对应扣0.5-1分的一般问题
- low：书写不规范等轻微问题

如无任何问题，issues为空数组，pass为true。
pass=false 表示存在任何值得修复的问题。"""

QC_FIX_PROMPT = """你是临床病历书写专家。根据以下质控问题，生成具体的修复文本，供医生直接写入病历。

质控问题：
字段：{field_name}
问题描述：{issue_description}
修改建议：{suggestion}

当前病历相关内容：
{current_record}

患者信息参考：
主诉：{chief_complaint}
现病史：{history}

请直接输出可以写入"{field_name}"字段的修复文本（不要包含字段名称标签，不要解释，直接给出内容）。
如果字段为空或无内容，请根据病历上下文生成合适内容。
输出格式：纯文本，简洁规范，符合医疗文书标准。

⚠️ 真实性红线（与批量修复口径一致）：客观测量数值**不得编造**——生命体征
（T/P/R/BP/SpO₂）、检验检查结果等数值，病历草稿和患者信息里没有的就不要虚构具体
数字。体格检查无数据写常规描述（如"神志清，查体合作，心肺腹未见明显异常"），
辅助检查无数据写"暂缺，建议完善XX检查"。诊断/治则治法可基于上下文合理推断，
但数值必须有出处。

⚠️ 阳性所见红线（2026-08-20 第三轮走查）：望闻切诊/舌脉象/体格检查类字段的修复文本
只能用**中性常规描述**（如"神志清，精神可，面色如常""舌淡红，苔薄白，脉平"），
**绝不编造病理性阳性所见**（如"面色少华""双目无神""舌暗有瘀斑"）——阳性体征必须
是医生亲自查见的，AI 无权替医生"查出"异常。

⚠️ 病史红线：既往史/过敏史/个人史/家族史类字段**不得编造"否认XX"式陈述**——
"否认"是患者亲口回答后才能写的。尤其绝不否认病历诊断中已出现的疾病（诊断含
"高血压病"却写"否认高血压史"是重大自相矛盾）。无数据时写"未详，待补充询问"。

⚠️ 质控规则关键词速查表（务必匹配，否则逐条修复后质控仍扣分）：
- field_name="现病史" → 文本必须含「治疗/服药/就诊/检查/用药/未治/未予/未行」任一关键词
  好写法示例：「…院外未予特殊治疗」「…自服布洛芬」「…社区医院就诊」「…未予用药」
- field_name="主诉" → 必须含「天/周/月/年/小时/分钟」任一时间单位
  好写法示例：「头痛 3 天」「腹痛 2 小时」"""


# ─── 批量补全 prompt（一次 LLM 调用返回 N 个字段的修复值） ──────────────────
#
# 设计哲学（2026-05-24 治本）：
#   旧 build_supplement_prompt 让 LLM 重写完整 JSON → renderer 整段重画
#   → 把医生现场修改物理上覆盖掉（bug：78 分逐条补全后整体补全回到 70）。
#
#   新方案：复用 QC_FIX_PROMPT 的"医生书写助手"风格，一次调用返回所有缺失
#   字段的建议值，前端按 FIELD_TO_LINE_PREFIX 行级写入 —— 与"逐条修复"
#   同一套机制，杜绝整段覆盖。
#
# 输入：质控问题清单 + 当前病历正文 + 患者上下文
# 输出：JSON {"items": [{"field_name": "舌象", "value": "..."}, ...]}
QC_FIX_BATCH_PROMPT = """你是临床病历书写专家。下面给出一份病历草稿和若干质控问题，请逐条生成可直接写入对应字段的修复文本。

病历类型：{record_type}

当前病历草稿：
{current_record}

患者信息参考：
主诉：{chief_complaint}
现病史：{history}

待修复字段清单（共 {issue_count} 项）：
{issues_block}

请严格按以下 JSON 格式输出（不要任何解释文字、不要 Markdown 代码块标记）：
{{
  "items": [
    {{"field_name": "字段名（原样照抄上方清单中的字段名）", "value": "可直接写入该字段的修复文本"}}
  ]
}}

硬性约束：
1. items 数组长度必须等于待修复字段清单条数，每条都要给出 value；不要新增字段名
2. field_name 必须从上方清单原样取，不要改写、合并、拆分
3. value 是该字段位置的最终写入内容（不含字段标签、不含"字段名："前缀、不含解释）
4. 如果某字段确实无明确依据，根据现病史/主诉/病历上下文给出合理临床推断（不要返回"无法判断"、不要留空、不要返回占位符 "[未填写，需补充]"）；但受第 7/8 条红线约束的字段除外——红线优先于本条
5. value 内容简洁规范，符合医疗文书标准（中文，不超过 100 字）
6. 真实性红线：客观测量数值**不得编造**——生命体征（T/P/R/BP/SpO₂）、检验检查结果等数值，
   病历草稿和患者信息里没有的就不要虚构具体数字。体格检查无数据时写常规描述
   （如"神志清，查体合作，心肺腹未见明显异常"），辅助检查无数据时写"暂缺，建议完善XX检查"。
   诊断/治则治法等临床判断可基于上下文合理推断，但数值必须有出处
7. 阳性所见红线（2026-08-20 第三轮走查）：望闻切诊/舌脉象/体格检查类字段只能写
   **中性常规描述**（如"神志清，精神可，面色如常""舌淡红，苔薄白，脉平"），
   **绝不编造病理性阳性所见**（如"面色少华""双目无神""舌暗有瘀斑"）——阳性体征
   必须是医生亲自查见的，AI 无权替医生"查出"异常
8. 病史红线：既往史/过敏史/个人史/家族史类字段**不得编造"否认XX"式陈述**——
   "否认"是患者亲口回答后才能写的。尤其绝不否认病历诊断中已出现的疾病（诊断含
   "高血压病"却写"否认高血压史"是重大自相矛盾）。无数据时写"未详，待补充询问"

⚠️ 质控规则关键词速查表（写以下字段时，value 中务必出现对应任一关键词，否则质控仍扣分）：
- **现病史** → 必须含「治疗 / 服药 / 就诊 / 检查 / 用药 / 未治 / 未予 / 未行」任一关键词（这是"诊治经过"判定，缺则扣 2 分）
  示例好写法：「…院外未予特殊治疗」「…自服布洛芬效果不佳」「…于社区医院就诊」「…未予用药」
- **主诉** → 必须含「天 / 周 / 月 / 年 / 小时 / 分钟」任一时间单位（持续时间判定，缺则扣分）
  示例好写法：「头痛 3 天」「腹痛 2 小时余」"""

