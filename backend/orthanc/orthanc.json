{
  // Orthanc 配置（backend/orthanc/orthanc.json）
  // 启动方式：docker compose up -d orthanc
  // 文档：https://orthanc.uclouvain.be/book/users/configuration.html

  // ───── 基本信息 ─────────────────────────────────────────
  "Name": "MediScribe-PACS",
  "DicomAet": "MEDISCRIBE",
  "HttpPort": 8042,
  "DicomPort": 4242,
  "DicomCheckCalledAet": false,
  "RemoteAccessAllowed": true,

  // ───── 鉴权（基础认证）─────────────────────────────────
  // FastAPI 调 Orthanc HTTP API 时用这个用户名密码做 Basic Auth
  // 前端 OHIF 也用同样凭证（短期方案；正式方案应该走 token 代理）
  "AuthenticationEnabled": true,
  "RegisteredUsers": {
    "mediscribe": "mediscribe-orthanc-2026"
  },

  // ───── DICOMweb 插件（核心，让 FastAPI/OHIF 走标准协议）─
  "DicomWeb": {
    "Enable": true,
    "Root": "/dicom-web/",
    "EnableWado": true,
    "WadoRoot": "/wado",
    "Ssl": false,
    "QidoCaseSensitive": false
  },

  // ───── PostgreSQL 索引（复用项目主 PG）──────────────────
  // 文件本身存文件系统（更快），元数据/索引存 PG（便于查询、备份与项目主库一致）
  "PostgreSQL": {
    "EnableIndex": true,
    "EnableStorage": false,
    "Host": "db",
    "Port": 5432,
    "Database": "orthanc",
    "Username": "medassist",
    "Password": "medassist123",
    "EnableSsl": false,
    "Lock": false
  },

  // ───── 文件存储 ─────────────────────────────────────────
  // /var/lib/orthanc/db 由 docker volume 挂载到宿主机
  "StorageDirectory": "/var/lib/orthanc/db",
  "IndexDirectory": "/var/lib/orthanc/db",

  // ───── CORS（让前端 OHIF 能直接 fetch）─────────────────
  "HttpHeaders": {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET,POST,PUT,DELETE,OPTIONS",
    "Access-Control-Allow-Headers": "Authorization,Content-Type,Accept,Origin,X-Requested-With"
  },

  // ───── 其他 ─────────────────────────────────────────────
  "ConcurrentJobs": 4,
  "JobsHistorySize": 30,
  "LogLevel": "default"
}
