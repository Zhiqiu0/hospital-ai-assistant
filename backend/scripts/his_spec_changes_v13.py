"""v1.3 规范变更清单（数据源，供生成 docx 与人读清单共用）。

来源：2026-08-14 四组规范一致性核对，共 51 条不一致。
其中属于代码错的已修（见 git log），本清单只列**需要改文档**的。

字段：
  loc     —— 规范里的位置（章节）
  what    —— 改了什么
  why     —— 为什么这样改
  origin  —— **为何此前未写明**（对厂商如实交代，责任在我方，
             不把变更表述成"你要重做工作"）
"""

CHANGES = [
    # ── A：请在贵方开发完成前对齐 ────────────────────────────────────────
    dict(
        prio="A", loc="3.2 record_type 枚举",
        what="枚举由 7 个补齐为 10 个，新增 senior_round（上级查房）、"
             "pre_op_summary（术前小结）、post_op_record（术后病程）",
        why="这三类文书住院医生在工作台下拉框里就能选，签发后回写的 record_type "
            "即为这三个字面量；枚举不全会导致这几类文书在贵方侧归入错误栏目。",
        origin="我方住院文书类型最初只规划了入院记录/病程/出院记录/手术记录四类，"
               "后按《病历书写基本规范》补齐了查房与围手术期文书，"
               "本规范的枚举当时未同步更新。",
    ),
    dict(
        prio="A", loc="3.3 刷新接口 target（含附录 A.3 示例）",
        what="target 取值改为「与本次写入的 record_type 完全一致」，"
             "示例由 outpatient_record 改为 outpatient（去掉 _record 后缀）",
        why="我方实际下发的就是 record_type 原值。取值对不上时，"
            "病历写入成功但刷新消息匹配不到，医生界面不会刷新。",
        origin="初版按「页面名」的思路设想 target，门诊场景下"
               "outpatient_record 恰好也说得通，因此未被发现；"
               "扩展到住院文书后才暴露该拼法不成立（会拼出 course_record_record），"
               "我方已统一为直接下发 record_type。",
    ),
    dict(
        prio="A", loc="2.2 公共请求头（含附录 A.2）",
        what="新增请求头 X-Request-Id：同一份文书的所有重发（含超时重试与"
             "我方补投）该值恒定，可作为一层请求级去重依据；不参与签名计算",
        why="网络超时时可能出现「贵方已写库、我方未收到响应」的情形，"
            "此时我方会重发。多这一个头，贵方可据此直接返回上次结果。",
        origin="这是我方本次新增的一层保护。此前仅依赖 2.5 的业务幂等键，"
               "规范中原本没有这个头，属能力增强而非贵方遗漏。",
    ),
    dict(
        prio="A", loc="7.2 握手鉴权失败的响应",
        what="由「HTTP 401」更正为「HTTP 403」（无 WebSocket close code）",
        why="我方在 WebSocket 升级完成之前就拒绝，服务器固定返回 403。"
            "这是握手期最主要的排障信号，取值不符会影响贵方定位问题。",
        origin="起草时按 HTTP 语义写成 401，未实测 WebSocket 握手阶段被拒时"
               "服务器的真实返回码。本次实测后更正。",
    ),
    dict(
        prio="A", loc="7.2 回写路由",
        what="补充：来源诊室连接失效时，我方会改投任意一条在线连接（msg_id 不变）；"
             "贵方任一客户端收到 record_writeback 请无条件写库",
        why="病历写的是全院共享库，与哪台诊室收到无关。"
            "若按「是不是本机接诊的」过滤，来源诊室掉线时这份病历会丢失。",
        origin="v1.2 定连接拓扑时写明了「路由回来源诊室」，"
               "但未写明来源连接失效时的兜底行为，属我方遗漏。",
    ),
    dict(
        prio="A", loc="7.4 补发与去重",
        what="按方向拆开说明：上行仍按 msg_id 去重；下行由我方补投任务发起的重投"
             "**会使用新的 msg_id**，请以 2.5 的业务幂等键为准判重",
        why="只按 msg_id 判重会在补投场景下产生重复病历。",
        origin="原文按「同一条连接内的重发」描述，"
               "未覆盖「连接断开后由补投任务重新发起」这一跨会话场景。",
    ),

    dict(
        prio="A", loc="3.1 接诊推送字段表（新增字段）",
        what="新增 patient_no（患者编号/病案号，跨就诊稳定）一项，建议必填",
        why="我方需要一个**跨就诊稳定**的患者标识来把同一个人的历次就诊归到同一份"
            "档案下。目前只能靠选填的 id_card / phone 判断，两者都缺时，"
            "同一位患者每来一次门诊就会新建一份档案——医生打开工作台看不到他的"
            "既往病历与过敏史，这在临床上是有风险的。"
            "只要该编号在贵方 HIS 里对同一个人稳定不变即可（病案号、患者主索引号、"
            "居民健康档案号都可以，用哪个由贵方定，告知我方取的是哪个即可）。"
            "我方仅用于档案查重，不做其它用途，也不会回写给贵方。",
        origin="本规范起草时把 visit_id（就诊流水号）作为唯一关联主键——"
               "它用于病历回写定位就诊是够的，但**患者档案层面缺一个稳定标识**，"
               "当时未意识到。我方代码里其实预留了这个字段，只是规范没写、"
               "贵方自然也不会推。这一条是我方设计上的疏漏。",
    ),

    # ── B：原要求已取消，贵方可少做 ──────────────────────────────────────
    dict(
        prio="B", loc="第 4 章 需贵方提供项",
        what="删除「① 诊断编码字典 ② 科室编码字典」——两份字典均无需提供",
        why="我方诊断以名称为准落库；科室以接诊推送随行的 dept_code + dept_name "
            "成对下发为准，不需要预先交换字典。",
        origin="第 4 章沿用了初版草案（当时设想按编码对齐）。"
               "v1.2 的附录 B 已确认无需字典，但第 4 章未同步删除，"
               "造成同一份文档内部口径不一致，是我方的疏漏。",
    ),
    dict(
        prio="B", loc="3.1 agent_device_ip / agent_device_mac",
        what="改为「选填，仅作排障留痕」；agent_device_mac 标注为保留字段、当前不使用",
        why="方案 B 下回写按 WebSocket 连接自动路由回来源诊室，不依赖这两个值。",
        origin="这两个字段是方案 A（诊室 Agent 中转）时代的设计。"
               "通道定为方案 B 后它们已失去用途，字段说明未同步。",
    ),
    dict(
        prio="B", loc="5.2 / 5.3 部署与网络",
        what="删除「诊室部署极薄 Agent 做内网中转」整段",
        why="方案 B 下我方不调用贵方任何接口、不在院内部署任何程序，"
            "贵方无需预留内网部署位。",
        origin="同上，方案 A 时代的方案。通道定稿后本节未整体重写。",
    ),
    dict(
        prio="B", loc="1.3 总体流程图",
        what="第 ② 步改为「我方自动建档并按 doctor_code 派入对应医生的工作台，"
             "医生在已登录的浏览器工作台点击进入」；要素由「设备IP」改为「医生工号」",
        why="双方均无需安装任何客户端程序，医生用的就是浏览器。",
        origin="流程图绘于方案 A 时期（含诊室 Agent 拉起嵌入工作台）。"
               "我方接入方式后续调整为纯浏览器，流程图未同步。",
    ),

    # ── C：口径补充，避免联调时产生分歧 ──────────────────────────────────
    dict(
        prio="C", loc="附录 A.2 回写示例",
        what="补上 record_no 与 full_text 两个顶层字段",
        why="这两个字段 3.2 字段表中本就有、我方每次都会下发；"
            "其中 record_no 是 2.5 幂等键的第三段，住院多份文书靠它区分。",
        origin="示例报文是初版手写的门诊样例，"
               "后续字段表新增这两项时示例未同步更新。",
    ),
    dict(
        prio="C", loc="3.2 status",
        what="写明我方**恒发 draft**：病历落贵方草稿/待确认态，"
             "医生可在 HIS 端继续编辑，点确认/保存后才真实落库；final 为后续预留",
        why="明确本期实际行为，避免为 final 分支做无谓准备。",
        origin="规范给出 draft/final 两个取值是为留扩展余地，"
               "但未写明本期我方实际只发 draft，属口径未写明。",
    ),
    dict(
        prio="C", loc="3.1 dept_code / dept_name",
        what="补充说明：我方按 dept_code 自动登记并**以此作为本次接诊的科室**"
             "（不是医生编制科室），请确保同一科室取值稳定",
        why="医生跨科室坐班时，病历上的科室须为本次挂号的科室。"
            "写明用途后，贵方能判断取值稳定性的重要程度。",
        origin="v1.2 已确认取科室表「本地ID」列，"
               "但未写明我方拿它做什么，贵方无从判断其重要性。",
    ),
    dict(
        prio="C", loc="3.1 id_card",
        what="维持选填，补充「强烈建议推送」及原因",
        why="我方跨就诊关联同一患者依赖身份证或「手机号+姓名」。"
            "两者都缺时，同一位患者每次就诊都会新建档案，"
            "医生将看不到其既往病历与过敏史。",
        origin="起草时按普通选填字段标注，"
               "未评估该字段缺失对患者档案关联的实际影响。",
    ),
    dict(
        prio="C", loc="3.1 幂等与补推",
        what="幂等键写明为 hospital_code + visit_id；"
             "补充「重复推送若带新 doctor_code 会自动改派医生」"
             "「体征与档案字段仅首次推送生效」",
        why="便于贵方安排推送时机——若护士补测体征后再推，"
            "该部分数据不会更新，建议首次推送时尽量带齐。",
        origin="幂等键起草时按单院场景只写了 visit_id，"
               "实际实现为支持多院隔离加了 hospital_code；"
               "补推行为则是业务联动上线后才形成的规则，均未回写规范。",
    ),
    dict(
        prio="C", loc="3.1 选填字段取值容错",
        what="补充：性别接受 male/female/unknown 与国标代码 1/2/9 与中文；"
             "初诊标志接受 bool 与代码 1/2；体征、身份证、手机号接受 JSON 数字",
        why="贵方按 HIS 原生类型推送即可，不必为迁就我方做额外的类型转换。",
        origin="这是我方本次新增的容错能力（此前对类型要求较严），"
               "属能力增强，为降低贵方对接成本。",
    ),
    dict(
        prio="C", loc="2.2 / 2.4 重试规则与错误码",
        what="标注 40006（nonce 重复）**仅适用于方案 A 与握手**；"
             "方案 B 的业务消息不做 nonce 去重，重发时 msg_id 保持不变",
        why="消除 2.2「重发须重新生成 nonce」与 7.4「重发 msg_id 不变」"
            "读起来互相矛盾的问题。",
        origin="2.2 是按方案 A（HTTP）起草的。"
               "通道定为方案 B 后，未区分两个通道在防重放机制上的差异。",
    ),
    dict(
        prio="C", loc="7.4 心跳",
        what="明确由**我方**每 30 秒下发 ping，贵方收到回 pong 即可（无需主动发起）",
        why="贵方不必实现心跳发起与空闲计时逻辑。",
        origin="起草时写「任一方」以示对称，实际实现为我方单向发起。",
    ),
    dict(
        prio="C", loc="第 6 章 联调与验收",
        what="按方案 B 重写验收步骤；并在最前面增加一步「医生名单交换」",
        why="原步骤中「我方 POST 到贵方接口」与 5.3「贵方无需对外开放接口」冲突。"
            "另：参与联调的医生工号需先在我方开户（一位医生的多个工号请一并提供），"
            "否则首条接诊推送会返回 40007。",
        origin="第 6 章沿用方案 A 的流程未重写；"
               "医生开户这一前置条件是我方业务联动上线后才产生的要求。",
    ),
    dict(
        prio="C", loc="附录 A.1 接诊示例",
        what="dept_code 示例由 0101 改为 1230024，dept_name 改为「全科」",
        why="与附录 B 已确认的取值口径、以及已交付的参考实现保持一致。",
        origin="示例用的是取值定稿前的占位值，定稿后未同步。",
    ),
    dict(
        prio="C", loc="2.5 字段语义",
        what="补充对象/数组型字段的空值语义：record / vitals / meta / diagnoses "
             "整体缺省或为空（{} / []）均表示**不更新该组**；"
             "并写明我方本期不使用空串语义，字段为空即不下发",
        why="避免 diagnoses 为空数组时被理解成「清空诊断」，"
            "把贵方已有的诊断抹掉。",
        origin="2.5 起草时只定义了标量字段的缺省/空串两态，"
               "未覆盖对象与数组；我方实际也未使用空串语义，一并写明。",
    ),
    dict(
        prio="C", loc="3.2 meta.generated_at / meta.doctor_code",
        what="generated_at 说明为「本次回写报文的生成时间（重发时会刷新）」；"
             "doctor_code 说明为「该就诊的接诊医生工号（取自 3.1 推送）」",
        why="写明取值来源，避免被当作其它口径入库统计。",
        origin="字段名沿用直觉命名，未写明取值来源，属说明不够precise。",
    ),
    dict(
        prio="C", loc="2.1 时间格式",
        what="补充「所有时间均为北京时间（UTC+8），不带时区后缀」",
        why="避免双方对时间基准产生分歧。",
        origin="起草时默认双方同处北京时间，未显式写明。",
    ),
]


def summary() -> str:
    lines = []
    for p, title in [("A", "请在贵方开发完成前对齐"),
                     ("B", "原要求已取消，贵方可少做"),
                     ("C", "口径补充，避免联调分歧")]:
        items = [c for c in CHANGES if c["prio"] == p]
        lines.append(f"【{p}】{title}（{len(items)} 项）")
        for c in items:
            lines.append(f"  · {c['loc']}")
    return "\n".join(lines)


if __name__ == "__main__":
    print(summary())
    print(f"\n合计 {len(CHANGES)} 项文档变更")
