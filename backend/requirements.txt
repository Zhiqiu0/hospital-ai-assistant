# 2026-07-07 安全升级：fastapi 0.115.0 钉死 starlette<0.39，而 starlette 0.38.6 有
# CVE-2024-47874（multipart 表单无大小限制内存耗尽 DoS，0.40.0 修复）。升到
# fastapi 0.115.6 放开到 starlette 0.41.x（含修复），登录/上传等未认证表单端点不再可被打。
fastapi==0.115.6
# 显式钉住 starlette（fastapi 0.115.6 允许 0.40~0.41）：0.41.3 含 CVE-2024-47874 修复
starlette==0.41.3
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg>=0.30.0
psycopg[binary]>=3.1.0
redis==5.1.1
pydantic==2.9.2
pydantic-settings==2.5.2
# 2026-06-11 安全升级：3.3.0 有 CVE-2024-33663（ECDSA 算法混淆）+
# CVE-2024-33664（JWE 解码炸弹 DoS），3.4.0 起修复
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# 2026-07-07 安全升级：0.0.12 有 CVE-2024-53981（畸形 boundary CPU 耗尽 DoS），0.0.18 修复
python-multipart==0.0.18
httpx==0.27.2
openai==1.51.0
python-dotenv==1.0.1
# 2026-06-11 钉死版本：未钉版本的依赖会让构建不可复现 + 有供应链风险
alibabacloud-bssopenapi20171214==5.1.1
pymupdf==1.27.2.2
numpy==2.4.3
pydicom==3.0.2
Pillow==12.1.1

# 中文姓名拼音索引（患者搜索框支持拼音/首字母/混拼，纯 Python 无 C 扩展）
# 仅在 PatientService 写入和查询时调用，运行时开销可忽略
pypinyin==0.53.0

# 错误监控（Sentry，FastAPI/SQLAlchemy/Logging 集成）
# 仅当环境变量 SENTRY_DSN 非空时初始化，开发环境默认不上报
sentry-sdk[fastapi]==2.18.0

# 测试依赖
pytest>=9.0
pytest-cov>=7.0  # 覆盖率报告（2026-06-11 加，找测试盲区用）
pytest-asyncio>=1.0
httpx>=0.27
aiosqlite>=0.20
